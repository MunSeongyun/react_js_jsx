# 🚀 React開発者のためのJavaScript核心ガイド

Reactは新しい言語ではなく、最新のJavaScript（ES6+）構文を積極的に活用するライブラリです。本ドキュメントは、ReactでのWeb開発における核心的な概念と、それを理解するために必ず知っておくべきJavaScriptの基礎・実戦構文をまとめたガイドです。

---

## 📌 0. React開発の核心：コンポーネント（Component）とは？

* **レゴブロックを組み立てる感覚**: ReactでWeb開発をする時は、画面全体を一度に作るのではなく、ボタン、ナビゲーションバー、コメント欄のような **「コンポーネント（部品）」を独立して作り、それらをレゴのように組み立てて** 一つの画面を完成させます。
* **コンポーネントの正体**: Reactにおけるコンポーネントとは決して大げさなものではなく、**HTMLとそっくりな見た目をした「JSX」を返す（returnする）だけのJavaScript関数**にすぎません。

> **💡 先取りサンプル**
> ```jsx
> // これがまさにReactの「コンポーネント」（関数）です！
> function WelcomeButton() {
>   return (
>     // HTMLのように見えますが、中身はJavaScriptである「JSX」
>     <button className="btn">クリックしてください</button> 
>   );
> }
>
