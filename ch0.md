# ⚛️ React開発のためのJavaScript(JS)必須基礎文法ガイドブック

本ドキュメントは、JavaScriptの基礎からReactのコンポーネント作成およびデータ処理に不可欠な必須文法のみを厳選してまとめたガイドです。

---

## 📂 目次

### Part 1. JavaScript言語自体のコア基礎文法
* **1. データ型 (Data Types)**
  * 文字列、数値、真偽値（Boolean）、null、undefined
* **2. 演算子 (Operators)**
  * 算術演算子 および 厳密等価演算子 (`===`, `!==`)
* **3. 条件分岐 (`if`, `else if`, `else`)**
* **4. オブジェクトと配列 (Objects & Arrays)**
  * オブジェクトと配列の基本概念およびデータへのアクセス方法
  * ⚠️ *重要: `const`で宣言したオブジェクトのプロパティ変更の原理 (メモリ参照の概念)*
* **5. ループ処理 (`for`文)**

### Part 2. React開発のための必須モダン(ES6+)文法
* **6. 変数宣言 (`const`, `let`)**
  * データの性質 (Props, State) に応じた変数の使い分け基準
* **7. アロー関数 (Arrow Functions)**
  * アロー関数の構文とReactコンポーネントでの活用
  * 💡 *なぜ使うのか？ (コードの簡潔化、可読性向上のメリット)*
* **8. 配列の高階関数 (`map`, `filter`)**
  * `map()`を活用したReactでの繰り返しレン더リング（ループ処理）
  * `filter()`を活用したデータ削除機能の実装
* **9. 三項演算子と短絡評価 (Conditional Rendering / 条件付きレンダリング)**
  * JSX内部での条件付きレンダリングに必須のパターン

### Part 3. 実務での活用度が高い応用基礎文法
* **10. オブジェクトのプロパティ短縮表記 (Property Shorthand)**
  * KeyとValueが同じ名前のときの省略表記
* **11. 非同期処理 (`async` / `await`)**
  * サーバーAPIからのデータ取得および例外処理の基礎
* **12. Truthy（真値）と Falsy（偽値）**
  * 条件式や短絡評価でよく使われるデータの真偽の性質
* **13. イベントリスナーとイベントオブジェクト (`e.target.value`)**
  * `e`オブジェクト（イベント情報が詰まった箱）の仕組みを理解する
  * Reactの`<input>`タグで入力値をStateに取り込む方法