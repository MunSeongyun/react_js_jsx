# ⚠️ 重要: 'const'で宣言したオブジェクトのプロパティ変更の原理 (メモリ参照の概念)

JavaScriptを学ぶ際、多くの人が最初に直面する疑問があります。
**「'const'は定数（変えられない値）のはずなのに、なぜオブジェクトや配列の中身は変更できるのか？」**

この仕組みを正しく理解するには、JavaScriptがバックグラウンドでどのようにメモリを管理しているか（参照値の概念）を知る必要があります。

---

## 1. 原則：できること と できないこと

* **できること (✅ 許可)**: オブジェクト内のプロパティの値を書き換えたり、新しいプロパティを追加・削除すること。
* **できないこと (❌ 禁止)**: 変数そのものに、新しいオブジェクトを丸ごと代入（再代入）すること。

```
const user = {
  name: "キム",
  age: 20
};

// 1. ✅ 可能: 内部のプロパティを変更・追加
user.age = 21;       // 値の書き換え
user.city = "ソウル"; // 新しいプロパティの追加
console.log(user);   // { name: "キム", age: 21, city: "ソウル" }

// 2. ❌ 不可: 変数そのものへの再代入
user = {
  name: "パク",
  age: 25
}; // TypeError: Assignment to constant variable. (エラーが発生)
```

---

## 2. メモリの仕組み（値型 と 参照型）

JavaScriptの変数は、データの種類によってメモリへの保存方法が異なります。

### ① 基本データ型（数値、文字列、真偽値など）＝「値型」
数値や文字列などの単純なデータは、変数という箱の中に**実際の値そのもの**が直接入ります。
そのため、'const'で宣言すると値そのものを変えることができなくなります。

### ② オブジェクト・配列（Object, Array）＝「参照型」
オブジェクトのような大きくて複雑なデータは、変数という箱の中に直接入りません。データはメモリ上の別の広い場所（ヒープ領域）に保存され、変数の中にはそのデータが置いてある**「メモリ上の住所（アドレス/参照値）」**だけが記録されます。

---

## 3. 'const'が本当に守っているものは？

> **'const'が保護しているのは、「変数の中身（＝メモリ上の住所）」だけであり、「住所の先にあるデータそのもの」ではありません。**

* 'user.age = 21;' と書いても、データの置いてある**住所（場所）は変わりません。** 住所が変わらないため、'const'のルールには違反しないのです（中身のデータだけが書き換わります）。
* しかし、'user = { ... };' のように新しいオブジェクトを代入しようとすると、新しいデータが作られ、**新しい住所に変わってしまいます。** 'const'は住所の変更を絶対に許さないため、ここでエラーが発生します。

---

## 4. 視覚的なイメージ

概念を分かりやすく例えると、**「家のリフォーム」と「引っ越し」の関係**に似ています。

* **プロパティの変更 ('user.age = 21') ＝ リフォーム**
  * 家の住所（メモリの場所）はそのままで、家の中の家具（データ）だけを新しくする行為です。'const'はこれを禁止しません。
* **変数の再代入 ('user = { ... }') ＝ 引っ越し**
  * まったく違う場所にある新しい家に移り住む（住所が変わる）行為です。'const'はこれを強力にブロックします。

---

## ⚛️ React開発における注意点

Reactでは、この「'const'でも中身が変わる」という性質があるにもかかわらず、**「State（状態）を更新するときは、元のオブジェクトを直接書き換えてはいけない（不変性/Immutabilityの維持）」**という重要なルールがあります。

Reactに「データが変わったよ！」と認識させるためには、中身だけを書き換える（リフォーム）のではなく、**スプレッド演算子（'...'）などを使って新しい住所のオブジェクト（引っ越し）を新しく作って渡してあげる**必要があります。この仕組みは、今後のReact学習で非常に重要になってきます。