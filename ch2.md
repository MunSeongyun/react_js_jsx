## Part 2. React開発のための必須モダン(ES6+)文法

ここからは、Reactのコードを読む・書く上で毎日のように遭遇するJavaScriptのモダンな文法（ES6以降）を学びます。これらの文法をマスターすることで、Reactのコンポーネントがどのように動いているかが明確に理解できるようになります。

---

### 6. 変数宣言 (`const`, `let`)
React開発では、扱うデータが「変化しないもの（Propsや固定の関数など）」か、「変化するもの（Stateなど）」かに応じて、変数の宣言を厳格に使い分けます。古い文法である `var` は現在使用されません。

* **`const`**: **再代入が不可能な定数**を宣言します。Reactのコンポーネント定義、Propsの受け取り、不変の関数を宣言する際に最も多く使用されます。
* **`let`**: **再代入が可能な変数**を宣言します。ループ処理の中や、値が後から書き換わる一時的な変数を扱う場合に使用します。

```
const componentName = "MyComponent"; 
// componentName = "NewComponent"; // ❌ エラー！ constは再代入できません。

let count = 0;
count = 1; //  可能！ letは再代入が許されています。
```

---

### 7. アロー関数 (Arrow Functions)
Reactでコンポーネントを定義したり、ボタンがクリックされたときのイベントハンドラー（処理）を書いたりするときに最もよく使われる関数の形です。

💡 **なぜ使うのか？（メリット）**
1. **コードが圧倒的に簡潔になる**: `function` という文字や、処理が1行だけの場合の `{}` と `return` を省略できるため、コードの可読性が上がります。
2. **`this` の挙動が直感的になる**: 従来の関数とは異なり、アロー関数は自身が定義された場所の周辺環境から `this` をそのまま引き継ぎます（Reactの過去のクラス型コンポーネントで発生しがちだった `this` のバグを防ぐ大きな役割を果たしました）。

```
// 従来の関数宣言
function sayHello() {
  return "こんにちは";
}

// アロー関数による宣言
const sayHelloArrow = () => "こんにちは"; // 処理が1行なので {} と return を省略可能

// Reactコンポーネントでの活用例
const Welcome = ({ name }) => {
  return <h1>こんにちは、{name}さん！</h1>;
};
```

---

### 8. 配列の高階関数 (`map`, `filter`)
Reactで**画面にリスト（繰り返しデータ）を表示したり、特定のデータを削除・抽出したりするとき**に必須となる配列専用の方法です。Reactではデータの表示に `for` 文ではなく、これらの高階関数を多用します。

#### 8.1 `map()` : 配列の全要素を処理して、新しい配列を作る（Reactの繰り返し文）
元の配列の中身を一つずつ取り出して変換し、同じ要素数の新しい配列を生成します。ReactではデータをJSX（HTMLのようなタグ）に変換する際に使います。

```
const items = ["りんご", "バナナ", "オレンジ"];

// Reactではこのように、データを1つずつの <li> タグに変換して画面にレンダリングします（keyの指定が必須）。
const itemList = items.map((item, index) => <li key={index}>{item}</li>);
```

#### 8.2 `filter()` : 条件に合う要素だけを抽出して、新しい配列を作る（削除機能の実装）
指定した条件を満たす（評価が `true` になる）要素だけを残した、新しい配列を生成します。

```
const todos = [
  { id: 1, text: "JSの勉強" },
  { id: 2, text: "Reactの勉強" }
];

// idが 1 ではないものだけを残す（＝idが1の項目を削除する処理として使われます）
const remainingTodos = todos.filter(todo => todo.id !== 1);
// 結果: [{ id: 2, text: "Reactの勉強" }]
```

---

### 9. 三項演算子と短絡評価 (Conditional Rendering)
ReactのJSX（画面表示部分）の内部で、条件に応じて画面の表示を切り替える（条件付きレンダリング）ための文法です。JSXの内部（`{ }` の中）では `if` 文や `switch` 文を直接書くことができないため、これらの文法が非常に重要になります。

#### 9.1 三項演算子 (`条件 ? 真のときの値 : 偽のときの値`)
条件が正しい場合とそうでない場合で、表示するコンポーネントや文字を切り替えたいときに使います。

```
const isLoggedIn = true;

// JSXの内部だと仮定します
<div>
  {isLoggedIn ? <button>ログアウト</button> : <button>ログイン</button>}
</div>
```

#### 9.2 短絡評価 / ショートサーキット評価 (`&&`, `||`)
* **`A && B`**: Aが真（`true`）であれば、Bを表示する（特定の条件のときだけ画面に表示したい場合に多用します）。
* **`A || B`**: Aが偽（`null` や `undefined` など）であれば、Bをデフォルト値として使用する。

```
// loading（読み込み中）が true のときだけ、スピナー（ローディング画像）を表示する
<div>
  {isLoading && <Spinner />}
</div>
```